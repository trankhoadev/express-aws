<!DOCTYPE html>
<html>
  <head>
    <title>Test</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css"
    />
  </head>

  <style>
    img {
      width: 50px;
    }
  </style>
  <body>
    <div class="container">
      <form
        action="/update?id=<%= product.maMay %>&price=<%= product.gia %>&brand=<%= product.hang %>&specific=<%= product.chiTiet %>&name=<%= product.ten %>"
        method="POST"
        enctype="multipart/form-data"
      >
        <div class="form-group py-2">
          <label for="">Product ID: </label>
          <input
            name="id"
            type="text"
            class="form-control"
            value="<%= product.maMay %>"
            disabled
          />
        </div>

        <div class="form-group py-2">
          <label for="">Price: </label>
          <input
            name="price"
            type="text"
            class="form-control"
            value="<%= product.gia %>"
          />
        </div>

        <div class="form-group py-2">
          <label for="">Brand: </label>
          <input
            name="brand"
            type="text"
            class="form-control"
            value="<%= product.hang %>"
          />
        </div>

        <div class="form-group py-2">
          <label for="">Specific: </label>
          <input
            name="specific"
            type="text"
            class="form-control"
            value="<%= product.chiTiet %>"
          />
        </div>

        <div class="form-group py-2">
          <label for="">Name: </label>
          <input
            name="name"
            type="text"
            class="form-control"
            value="<%= product.ten %>"
          />
        </div>

        <div class="d-flex justify-content-center py-2">
          <button class="btn btn-success submit">Update</button>
        </div>
      </form>
    </div>
  </body>
</html>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"></script>
